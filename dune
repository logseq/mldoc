(include_subdirs unqualified)

(executable
  (name org_parser)
  (preprocess (pps js_of_ocaml-ppx))
  (libraries angstrom js_of_ocaml))

(alias
  (name DEFAULT)
  (deps org_parser.exe))

(alias
  (name JS)
  (deps org_parser.bc.js))
